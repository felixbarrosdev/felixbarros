document.addEventListener('DOMContentLoaded', () => {
  if (typeof Typed !== 'undefined') {
    new Typed('#typed-dev', {
      strings: ['DEV'],
      typeSpeed: 80,
      backSpeed: 40,
      loop: true
    });
  }
});
